[project]
name = "qgen"
version = "0.1.0"
description = "Local questionnaire generator (qGen) for per-recipient survey variants."
requires-python = ">=3.11"
dependencies = []
license = {text = "MIT"}
authors = [{name = "Isaac Kohane"}]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


